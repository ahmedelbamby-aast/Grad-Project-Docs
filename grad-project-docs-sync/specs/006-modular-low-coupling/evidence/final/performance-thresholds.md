# Performance Thresholds

Frontend modular performance tests passed:

`npm test -- tests\integration\modular-performance.test.ts --run`

The tests validate live frame propagation and health/dashboard store updates under the local thresholds. Backend real-data throughput validation is blocked by the model/runtime and raw-media gaps recorded in the real-data evidence files.
