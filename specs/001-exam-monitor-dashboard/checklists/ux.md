# UX / UI Requirements Quality Checklist: Exam Monitoring Dashboard

**Purpose**: Validate the completeness, clarity, consistency, and measurability of all UX/UI requirements — themes, layouts, interactions, accessibility, component library, and visual overlays.
**Created**: 2026-02-27
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)
**Depth**: Thorough | **Audience**: QA / Release Gate

---

## Requirement Completeness

- [x] CHK001 — Are visual design specifications (spacing, typography scale, border radii, shadow depths) defined for the reusable component library beyond the component list in FR-033? [Completeness, Gap] *(Resolved in spec-addendum §1.1: Complete spacing scale (4/8/12/16/24/32/48px), typography scale (12-32px), border radii (4/8/12/9999px), shadow depths (sm/md/lg/xl) with CSS custom property names.)*
- [x] CHK002 — Are exact CSS custom property token names and values documented for all three themes (Black+Purple, Full Black, White), or only sample tokens shown in research R-009? [Completeness, Spec §FR-021] *(Resolved in spec-addendum §1.1: Complete token tables for all 3 themes with --color-bg-primary, --color-bg-secondary, --color-accent, --color-text-primary, etc. All hex values specified.)*
- [x] CHK003 — Are layout specifications defined for each of the ~8 pages (Login, Dashboard, Camera Feed, Predictions Panel, Recordings, Recording Playback, Health Dashboard, Settings)? [Completeness, Gap] *(Resolved in spec-addendum §3.1-§3.8: All 8 page layouts specified with sections, content areas, and component placement.)*
- [x] CHK004 — Are empty state / zero-data designs specified for all pages (e.g., no cameras connected, no recordings, no anomalies, no students detected)? [Completeness, Gap] *(Resolved in spec-addendum §4.1: Empty states table for 6 views with icon, title, description, action button.)*
- [x] CHK005 — Are loading state visual designs (skeleton screens, spinners, progress bars) specified for each asynchronous operation listed in FR-034? [Completeness, Spec §FR-034] *(Resolved in spec-addendum §4.2: Loading states — skeleton screens for page loads, LoadingSpinner for actions, progress bar for exports. Consistent pattern across all async operations.)*
- [x] CHK006 — Are error state visual designs defined for camera connection failure, WebSocket disconnection, detection service unavailable, and storage warnings? [Completeness, Spec §FR-036] *(Resolved in spec-addendum §4.3: Error states table for 8 scenarios with icon, message, action, and component type (StatusBanner/overlay/toast).)*
- [x] CHK007 — Are animation/transition specifications defined for theme switching beyond the 300ms timing in FR-022 (easing curve, which properties animate, stagger order)? [Completeness, Spec §FR-022] *(Resolved in spec-addendum §1.2: ease-in-out easing, properties: background-color, color, border-color, box-shadow. No stagger — all properties transition simultaneously via CSS custom properties.)*
- [x] CHK008 — Are notification/toast requirements defined for transient feedback (e.g., "Camera connected", "Anomaly acknowledged", "Recording started")? [Completeness, Gap] *(Resolved in spec-addendum §4.4: Toast system — position bottom-right, max 3 visible, FIFO queue. Trigger list for all transient feedback including camera, anomaly, recording, and export events.)*
- [x] CHK009 — Are iconography requirements specified — which icon library, icon set, and icon-to-meaning mapping for status indicators (connected, disconnected, recording, error)? [Completeness, Gap] *(Resolved in spec-addendum §1.3: Lucide React icons (already in plan). Icon mapping: CheckCircle=connected, XCircle=disconnected, Circle(red)=recording, AlertTriangle=error, Wifi/WifiOff=WS status.)*
- [x] CHK010 — Are focus indicator styles defined for keyboard navigation across all three themes? [Completeness, Spec §FR-035] *(Resolved in spec-addendum §2.3: Focus ring — 2px solid var(--color-focus-ring) with 2px offset. Per-theme: Black+Purple=#7C3AED, Full Black=#A78BFA, White=#6D28D9. High contrast against all backgrounds.)*
- [x] CHK011 — Are requirements defined for the responsive grid breakpoints when camera count changes (1, 2, 3, 4, 5, 6, 7, 8 cameras)? [Completeness, Spec §FR-007] *(Addressed: FR-007 defines complete breakpoint table: 1→Full width, 2→1×2, 3→2×2 one empty, 4→2×2, 5-6→2×3, 7-8→2×4. All 8 camera counts mapped.)*
- [x] CHK012 — Is the visual design for the "Connection Lost — Reconnecting..." camera tile overlay specified (colors, icon, animation, progress indicator for retry count)? [Completeness, Spec §FR-010] *(Resolved in spec-addendum §6.1: Semi-transparent dark overlay (rgba(0,0,0,0.7)), WifiOff icon, "Reconnecting... (3/5)" text, pulse animation on icon. After max retries: static "Connection Lost" with retry button.)*
- [x] CHK013 — Are hover, focus, active, and disabled states defined for all interactive components in the component library (Button, Card, Modal, Toggle, StatusBadge)? [Completeness, Spec §FR-033] *(Resolved in spec-addendum §2.1: All component states documented — Button: hover(brighten 10%), active(scale 0.98), disabled(opacity 0.5, cursor not-allowed). Card: hover(shadow-md). Toggle: on/off states with transition.)*
- [x] CHK014 — Is the visual design for the anomaly timeline markers on the recording playback scrubber specified (marker shape, size, severity color coding, hover tooltip)? [Completeness, Spec §FR-029] *(Resolved in spec-addendum §3.6: Markers as circles (6px) on timeline. Colors: red=high, yellow=medium, blue=low. Hover: tooltip with anomaly type + timestamp. Click: seek to that position.)*
- [x] CHK015 — Are requirements defined for the predictions panel layout — how per-student cards are arranged, sorted, and scrolled when tracking 20+ students simultaneously? [Completeness, Spec §FR-016] *(Resolved in spec-addendum §3.4: Cards in vertical scroll list, sorted by tracking_id ascending. Max visible: ~8 cards, scroll for overflow. Search by tracking_id. Compact mode: 2-column grid for ≥20 students.)*

## Requirement Clarity

- [x] CHK016 — Is "visually striking login page" (User Story 1) quantified with specific design criteria (hero imagery, animation, brand elements, color palette usage)? [Clarity, Spec §US-1] *(Resolved in spec-addendum §3.1: Login page — centered card on gradient background (theme-aware), logo/brand mark at top, form fields (username + password), "Sign In" button, theme accent color prominent. No hero imagery — clean/minimal.)*
- [x] CHK017 — Is "responsive grid layout" (FR-007) defined with specific grid behavior rules — does 3 cameras use 2x2 with one empty, 1x3, or auto-fit? [Clarity, Spec §FR-007] *(Addressed: FR-007 explicitly states "3 → 2×2 (one empty cell)." Unambiguous.)*
- [x] CHK018 — Is "visually de-emphasized" for acknowledged anomaly alerts (FR-041) quantified — reduced opacity, muted colors, smaller size, or moved to a separate section? [Clarity, Spec §FR-041] *(Resolved in spec-addendum §5.2: Acknowledged = opacity 0.7 + muted badge color. Not moved to separate section. Dismissed = opacity 0.5 + strikethrough on title.)*
- [x] CHK019 — Is "distinct visual indicators" for anomaly status (FR-044) defined with specific icon, color, and badge design per status (new/acknowledged/dismissed) per theme? [Clarity, Spec §FR-044] *(Resolved in spec-addendum §5.2: New=red dot + "New" badge, Acknowledged=yellow check + "Ack" badge at opacity 0.7, Dismissed=gray dash + "Dismissed" badge at opacity 0.5. Same across all themes.)*
- [x] CHK020 — Are "purple accent colors on a dark background" (User Story 5, scenario 1) specified with exact color values, not just descriptive words? [Clarity, Spec §US-5] *(Addressed: Research R-009 provides exact hex values: accent #7C3AED, bg-primary #1A1A2E, bg-secondary #16213E, text-primary #E0E0E0. Full Black theme: bg-primary #000000, accent #7C3AED. White theme: bg-primary #FFFFFF, bg-secondary #F8F8F8, accent #7C3AED.)*
- [x] CHK021 — Is "severity indicator (e.g., red border for high severity, yellow for medium)" (User Story 4, scenario 2) confirmed as the final design or just an example? Is low severity color defined? [Clarity, Spec §US-4] *(Resolved in spec-addendum §1.1: Confirmed as final. High=red (#EF4444), Medium=yellow (#F59E0B), Low=blue (#3B82F6). Low severity color now defined.)*
- [x] CHK022 — Is "prominent" or "highlighted" for the clicked-anomaly student bounding box (FR-018) defined with measurable visual treatment (pulsing, thicker stroke, glow, zoom)? [Clarity, Spec §FR-018] *(Resolved in spec-addendum §5.7: 3px border + box-shadow glow (0 0 8px accent color). Pulse animation 1s ease-in-out infinite. Auto-dismiss after 5s. Click again to dismiss early.)*
- [x] CHK023 — Is the "filter panel" (FR-013) specified as a sidebar, popover, overlay, or inline panel? Is its position relative to the camera feed defined? [Clarity, Spec §FR-013] *(Resolved in spec-addendum §6.5: Inline toggle bar above the camera grid. Horizontal row of class-name toggle buttons. Not a sidebar/popover/overlay.)*
- [x] CHK024 — Is "actionable error messages" (FR-036) defined with a message template or pattern (what happened + suggested action), or left to implementer interpretation? [Clarity, Spec §FR-036] *(Resolved in spec-addendum §4.3: Error template — "[What happened]: [Brief explanation]. [Suggested action]." Example: "Connection Lost: WebSocket disconnected. Reconnecting automatically...")*
- [x] CHK025 — Is "auto-adjusts based on the number of feeds and available screen space" (User Story 2, scenario 3) defined with specific breakpoint rules or algorithmic behavior? [Clarity, Spec §US-2] *(Addressed: FR-007 provides the exact algorithm via the breakpoint table. US-2 scenario 3 references auto-adjustment; FR-007 defines the deterministic grid mapping. No ambiguity.)*

## Requirement Consistency

- [x] CHK026 — Are bounding box color assignments (FR-011, FR-014, FR-025) consistent across the spec — "purple for students, gray for teachers" (US-2) vs "distinct colors per class" (FR-011) vs "cyan for students, purple for teachers" (research R-009)? [Consistency, Spec §FR-011 vs §FR-014 vs R-009] *(Resolved in spec-addendum §1.1: R-009 is canonical — cyan #22D3EE for students, purple #A78BFA for teachers. US-2 description is superseded by the research-backed decision.)*
- [x] CHK027 — Are severity color semantics consistent between the anomaly alert display (FR-017: red=high, yellow=medium) and the constitution color standard (red=alert/critical, yellow=warning, green=normal)? Is low severity mapped? [Consistency, Spec §FR-017 vs Constitution §III] *(Resolved in spec-addendum §1.1: Consistent — red=high/critical, yellow=medium/warning. Low=blue (#3B82F6) — not green (which means "normal/healthy"). Green reserved for success/healthy status only.)*
- [x] CHK028 — Is the theme toggle location consistent — "settings menu or quick-toggle in the header" (US-5) vs the plan's `ThemeToggle.tsx` in layout/header? Is one canonical location defined? [Consistency, Spec §US-5 vs Plan] *(Resolved in spec-addendum §2.2: Header is canonical location — ThemeToggle in header for quick access. Settings page also has theme selection for discoverability. Both valid, header is primary.)*
- [x] CHK029 — Are loading state patterns consistent across all pages — does Camera Feed use the same loading component as Recordings and Health Dashboard? [Consistency, Spec §FR-034] *(Resolved in spec-addendum §4.2: Yes — same LoadingSpinner component used across all pages. Skeleton screens for list pages (same Skeleton component). Consistent loading UX.)*
- [x] CHK030 — Are card component requirements consistent between the predictions panel `StudentCard`, the recordings list `RecordingList`, and the anomaly `AnomalyAlert` — do they all use the shared `Card` from the component library? [Consistency, Plan Structure] *(Resolved in spec-addendum §2.1: Card is the base component. StudentCard, RecordingCard, and AnomalyAlert compose Card with domain-specific content. Consistent base styling + padding + shadow.)*
- [x] CHK031 — Is the "Connection Lost — Reconnecting..." UI treatment (FR-010, edge case) consistent with the "Detection Service Unavailable — Raw Feed Only" banner (edge case) — are both using the same status banner component? [Consistency, Spec Edge Cases] *(Resolved in spec-addendum §4.3: Both use StatusBanner component. Connection Lost = warning variant (yellow). Detection Unavailable = info variant (blue). Same component, different variant.)*

## Acceptance Criteria Quality

- [x] CHK032 — Can the 300ms theme switch requirement (FR-022, SC-005) be objectively measured — is there a defined measurement method (first-paint timing, DOM mutation observer, visual diff)? [Measurability, Spec §SC-005] *(Resolved in spec-addendum §1.2: Measured via Performance.mark() around data-theme attribute change + getComputedStyle verification. CSS transition duration is the mechanism — 300ms transition property.)*
- [x] CHK033 — Can "200ms filter toggle effect" (FR-015, SC-003) be objectively measured — from input event to canvas redraw complete, or from click to visual absence of boxes? [Measurability, Spec §SC-003] *(Resolved in spec-addendum §13.2: Measured from pointerup/keyup event to next paint (via PerformanceObserver). Canvas redraw included — full pipeline from input to visual update.)*
- [x] CHK034 — Can "90% of instructors" first-attempt camera success (SC-006) be measured — is there a usability test plan, sample size, or observability metric defined? [Measurability, Spec §SC-006] *(Addressed: Clarification Round 3 states SC-006 is validated by "qualitative validation using SUS (System Usability Scale) questionnaire — target SUS score ≥ 68 (industry average)." Measurement methodology defined.)*
- [x] CHK035 — Can WCAG 2.1 AA compliance (FR-024, SC-010) be objectively verified — are specific contrast ratios documented, or is automated tooling (axe-core, Lighthouse) mandated? [Measurability, Spec §SC-010] *(Addressed: Plan mandates axe-core for accessibility testing. SC-010 states "Automated axe-core + manual screen-reader validation." Tooling is specified.)*
- [x] CHK036 — Is "within 2 seconds" for anomaly alert display (SC-007) measured from backend detection event timestamp to frontend panel render, and is this timing verifiable end-to-end? [Measurability, Spec §SC-007] *(Addressed: Clarification Round 2 Feb 28: "SC-007 timing target refers to the interval starting at the moment the YOLO model inference first identifies the anomalous event and ending when the anomaly alert is rendered in the instructor’s browser." End-to-end timing defined.)*

## Scenario Coverage

- [x] CHK037 — Are requirements defined for what the UI shows during the brief gap between opening a camera feed and receiving the first detection results from the backend? [Coverage, Gap] *(Resolved in spec-addendum §4.1: "Waiting for detections..." message with subtle pulse animation on empty canvas. Video feed visible underneath.)*
- [x] CHK038 — Are UI requirements defined for multi-tab behavior — when an instructor has the same dashboard open in two tabs, how do anomaly triage actions sync visually? [Coverage, Spec Edge Case §multi-tab] *(Resolved in spec-addendum §8.5: Independent WS connections per tab. Both receive anomaly.status_change broadcasts. Triage sync is automatic via WS. Theme sync via localStorage storage event.)*
- [x] CHK039 — Are requirements defined for the recording playback controls (play, pause, seek, speed, volume/mute, fullscreen) and their visual design? [Coverage, Spec §FR-028] *(Resolved in spec-addendum §9.1: Full controls — play/pause button, seek bar with progress, speed selector (0.5x/1x/1.5x/2x), volume slider with mute toggle, fullscreen button. Bottom toolbar layout.)*
- [x] CHK040 — Are UI requirements defined for the camera management dialog — form validation states, error messages for invalid RTSP URLs, and loading state during connection attempt? [Coverage, Spec §US-7] *(Resolved in spec-addendum §16.4: RTSP URL regex validation with inline error. Name: required, 3-100 chars. Loading: spinner + "Connecting..." during attempt. Success: toast + dialog closes. Failure: inline error with retry.)*
- [x] CHK041 — Are requirements defined for the alert history view layout (FR-045) — table vs list vs timeline, filtering/sorting options, pagination? [Coverage, Spec §FR-045] *(Resolved in spec-addendum §5.1: Table layout with columns: timestamp, severity, type, camera, status, actions. Sort by any column. Filter by severity/type/camera/date. 20 items/page pagination.)*
- [x] CHK042 — Are requirements defined for the Health Dashboard page layout — how are the 5 metrics (API status, WebSocket health, FPS, storage, sessions) arranged and visualized (gauges, bars, status lights)? [Coverage, Spec §FR-038] *(Resolved in spec-addendum §3.7: Grid of status cards — each with status light (green/yellow/red circle), metric label, current value, and trend indicator. 2×3 grid on desktop. 10s auto-refresh.)*
- [x] CHK043 — Are requirements defined for the dismiss reason input (FR-042) — inline text field, modal dialog, character counter, validation feedback for the 5-character minimum? [Coverage, Spec §FR-042] *(Resolved in spec-addendum §5.5: Inline textarea below the anomaly alert, expands on "Dismiss" click. Character counter showing current/min (e.g., "3/5"). Submit disabled until ≥5 chars. Error message: "Reason must be at least 5 characters.")*
- [x] CHK044 — Are requirements defined for the anomaly annotation (FR-043) — how and where does the instructor add timestamped notes? Is there a note list below each alert? [Coverage, Spec §FR-043] *(Resolved in spec-addendum §5.6: Expandable notes section below each anomaly alert. "Add Note" button opens textarea. Notes display as timestamped list (newest first) with instructor name. Notes persist via POST /api/v1/anomalies/{id}/notes/.)*

## Edge Case Coverage

- [x] CHK045 — Are visual requirements defined for when a student's tracking ID changes mid-session (re-identification failure edge case) — how is the instructor notified visually? [Edge Case, Spec Edge Case §re-identification] *(Resolved in spec-addendum §12.9: Backend sends tracking_update message. Frontend: old StudentCard fades out (300ms), new card inherits history. Toast: "Student re-identified: #{old_id} → #{new_id}". Canvas: smooth bbox transition.)*
- [x] CHK046 — Are requirements defined for the bandwidth degradation warning UI — where does it appear, what does it say, can the instructor take action from the warning? [Edge Case, Spec Edge Case §bandwidth] *(Resolved in spec-addendum §12.10: StatusBanner at top of camera feed tile: "Low Bandwidth — Frame rate reduced." No user action available — resolution reduction is backend-controlled. Badge auto-clears when FPS recovers.)*
- [x] CHK047 — Are requirements defined for the storage capacity warning UI at 80% and 95% (FR-031) — persistent banner, dismissable toast, or blocking modal? What actions are offered? [Edge Case, Spec §FR-031] *(Addressed: Clarification Round 3: "80% threshold — persistent warning banner. 95% threshold — blocking modal requiring admin acknowledgment." UI treatment for both thresholds explicitly defined.)*
- [x] CHK048 — Are visual requirements defined for the "No students detected in frame" state — does the predictions panel show a placeholder, empty message, or collapse? [Edge Case, Spec Edge Case §no-students] *(Addressed: Edge case §No students in frame: "predictions panel MUST display a ‘Waiting for detections…’ placeholder." Explicit visual requirement.)*
- [x] CHK049 — Is the visual treatment defined for mutually exclusive prediction constraint violations (FR-020) — how does the UI indicate which prediction is invalid or conflicting? [Edge Case, Spec §FR-020] *(Resolved in spec-addendum §12.11: Warning badge on affected StudentCard — orange triangle icon with tooltip "Conflicting predictions detected." Server enforces constraints; frontend displays warning only.)*
- [x] CHK050 — Are requirements defined for the session reconnection UI when a browser crash is recovered — what visual feedback does the instructor see upon reconnection? [Edge Case, Spec Edge Case §browser-crash] *(Resolved in spec-addendum §12.12: On page reload, auth cookie persists. Dashboard loads normally. Active session detected → auto-navigate to camera feed. Toast: "Session restored — reconnecting to cameras..." WS auto-reconnects.)*

## Non-Functional Requirements (UX-Specific)

- [x] CHK051 — Are WCAG 2.1 AA contrast ratio targets documented for specific element types (body text ≥4.5:1, large text ≥3:1, UI components ≥3:1) across all three themes? [Accessibility, Spec §FR-024] *(Resolved in spec-addendum §11.1: Per-element targets documented. Verification via axe-core per-theme CI run. All 3 themes tested.)*
- [x] CHK052 — Are screen reader / ARIA requirements defined for live-updating detection counts, anomaly alerts, and camera status changes? [Accessibility, Gap] *(Resolved in spec-addendum §11.2: ARIA live regions — anomaly alerts=assertive, detection counts=polite, camera status=assertive, toast=polite, export progress=polite.)*
- [x] CHK053 — Are keyboard shortcut requirements defined for high-frequency actions (toggle filter, acknowledge alert, switch camera, play/pause recording)? [Accessibility, Spec §FR-035] *(Resolved in spec-addendum §11.3: Keyboard shortcuts table — Ctrl+1-8=camera focus, Space=play/pause, A=acknowledge alert, D=dismiss alert, F=toggle filter panel, Esc=close modal, ?=show help.)*
- [x] CHK054 — Are requirements defined for reduced-motion preferences — should theme transitions and bounding box animations respect `prefers-reduced-motion`? [Accessibility, Gap] *(Resolved in spec-addendum §11.5: Yes — all animations gated by prefers-reduced-motion. Instant theme switch, no pulse glow, static spinner replaced with text.)*
- [x] CHK055 — Is the 1920×1080 minimum resolution requirement (FR-037) specified with behavior for smaller viewports — does it clip, scroll, warn, or scale down? [Responsiveness, Spec §FR-037] *(Resolved in spec-addendum §16.6: Below 1920×1080 — horizontal scroll allowed. Below 1280px width — sidebar collapses to hamburger menu. Below 768px — single-column layout. No blocking modal for small screens.)*
- [x] CHK056 — Are performance requirements for Canvas overlay rendering defined — maximum number of concurrent bounding boxes before frame drops, and degradation strategy? [Performance, Gap] *(Resolved in spec-addendum §6.3: Max 200 bboxes total. Beyond 200: skip lowest-confidence. Per-camera cap = 200/active_cameras. Below 10 FPS: reduce to top-10-confidence per camera.)*
- [x] CHK057 — Is the initial page load budget (<3s) broken down per page with specific asset budgets (JS bundle, CSS, fonts, initial API calls)? [Performance, Plan Technical Context] *(Resolved in spec-addendum §13.3: Budget — 500KB JS (gzipped), 50KB CSS, 30KB fonts. TTI <3s on 4G. Code splitting per route. Lazy load non-critical pages.)*

## Dependencies & Assumptions

- [x] CHK058 — Is the assumption that "bounding box overlay colors are adaptive via CSS variables" (research R-009) validated — can Canvas stroke colors actually read CSS custom properties in realtime, or does additional JS bridging logic need to be specified? [Assumption, R-009] *(Resolved in spec-addendum §6.2: Canvas cannot read CSS vars directly. JS bridging: getComputedStyle() reads --color-class-* tokens, cached in JS Map. MutationObserver on data-theme attr invalidates cache on theme change.)*
- [x] CHK059 — Is the dependency on go2rtc's `<video>` element confirmed to support Canvas overlay positioning across all target browsers (Chrome, Firefox, Edge)? [Dependency, R-011] *(Resolved in spec-addendum §6.2: go2rtc WHEP delivers MediaStream to <video>. Canvas overlays positioned absolutely on top. Works cross-browser — standard <video> + <canvas> composition. Verified by R-011 research.)*
- [x] CHK060 — Is the assumption that Radix UI / React Aria provides WCAG 2.1 AA headless components validated — are specific component primitives listed for modals, dropdowns, and toggles? [Assumption, R-002] *(Resolved in spec-addendum §15.1: Radix UI validated in R-002. Primitives: Dialog (modal), DropdownMenu, Toggle, Select, Tooltip, AlertDialog. All provide ARIA attributes + keyboard handling out of the box.)*

## Ambiguities & Conflicts

- [x] CHK061 — Does the spec define the dashboard page content (DashboardPage.tsx in plan) — what does the instructor see after login? Is it a summary page, direct to camera feed, or a navigation hub? [Ambiguity, Gap] *(Addressed: FR-046: "real-time dashboard summary: active session count, total connected cameras, recent anomaly count, system health status indicator." It is a summary page with live metrics.)*
- [x] CHK062 — Does the spec define the Settings page content (SettingsPage.tsx in plan) beyond theme switching — instructor profile, notification preferences, session timeout config? [Ambiguity, Gap] *(Addressed: Settings page covers: theme switching (FR-023), profile update (PATCH /auth/me/ — name/email), password change (FR-070). Notification preferences and timeout config are not in scope per spec.)*
- [x] CHK063 — The spec says "cameras are system-wide shared resources" but the plan shows `instructor_id` FK on CameraSource — does the UI show all cameras or only the instructor's cameras? Is there a global camera list view? [Conflict, Spec §FR-009 vs Data Model §CameraSource] *(Addressed: Clarification Round 2 Feb 28: "Cameras are system-wide shared resources — all authenticated instructors can view and select from the complete pool. Each instructor creates their own ExamSession and selects from the shared pool." data-model.md CameraSource has no instructor_id FK. Conflict resolved.)*
- [x] CHK064 — Is the double-click-to-fullscreen camera behavior (US-7, scenario 2) specified with a visual affordance — how does the instructor discover this interaction without documentation? [Ambiguity, Spec §US-7] *(Resolved in spec-addendum §6.6: Fullscreen affordance — expand icon (Maximize2 from Lucide) visible on camera tile hover at top-right corner. Double-click also works. Tooltip: "Double-click to fullscreen.")*
- [x] CHK065 — Are the "clickable markers on a timeline" (FR-029) defined with interaction behavior — do they show a tooltip on hover? Do they snap playback? Are they draggable? [Ambiguity, Spec §FR-029] *(Resolved in spec-addendum §3.6: Tooltip on hover showing anomaly type + timestamp. Click snaps playback to marker position. Not draggable — markers are fixed position indicators.)*

---

## Notes

- Check items off as completed: `[x]`
- Add findings or comments inline below each item
- Reference specific spec sections using `[Spec §FR-XXX]` notation
- Items marked `[Gap]` indicate requirements that appear to be missing entirely
- Items marked `[Ambiguity]` indicate requirements that need clarification before implementation
- Items marked `[Conflict]` indicate requirements that contradict each other across documents
- Total items: 65 | Traceability rate: 95% (62/65 items reference spec sections, gaps, or research)

