# Frontend Domain Checklist: Exam Monitoring Dashboard

**Purpose**: Validate completeness, clarity, and consistency of all frontend-related requirements across spec.md, plan.md, tasks-frontend.md, and API contracts — as a peer-review gate before implementation begins.
**Created**: 2026-02-27
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md) | [tasks-frontend.md](../tasks-frontend.md)
**Depth**: Thorough | **Audience**: Reviewer (PR/peer review)

**Note**: This checklist tests the **quality of the requirements**, not the implementation. Each item asks whether the spec/plan/contracts are complete, clear, consistent, and measurable for the frontend domain.

---

## Component Architecture Completeness

- [x] CHK001 — Are prop interfaces and accepted state variants documented for all 7 UI library components (Button, Card, Modal, StatusBadge, FormInput, Toggle, LoadingSpinner), or is the implementer left to infer them? [Completeness, Spec §FR-033, Plan §frontend/src/components/ui/] *(Resolved in spec-addendum §2.1: Full prop interfaces and state variants for all 7 components.)*
- [x] CHK002 — Are composition rules defined for how layout components (Header, Sidebar, AppLayout) nest together — specifically whether Sidebar is always visible, conditionally rendered, or collapsible by default? [Completeness, Gap] *(Resolved in spec-addendum §2.2: AppLayout = Header + Sidebar + main content. Sidebar always visible ≥1200px, collapsible <1200px via hamburger.)*
- [x] CHK003 — Is the StudentCard component's data shape fully specified — which pyramid prediction fields are displayed, how confidence values are visualized (bars, percentages, icons), and what "compact vs expanded" view modes contain? [Completeness, Tasks §T072, Spec §FR-016] *(Resolved in spec-addendum §3.4: StudentCard shows tracking_id, class_label, confidence bar (0-100%), bbox thumbnail. Compact=name+confidence. Expanded=all predictions+history chart.)*
- [x] CHK004 — Are requirements defined for React error boundary placement — which component tree levels catch errors, what fallback UIs are shown, and whether errors are reported to the backend? [Completeness, Gap] *(Addressed: FR-036 specifies "React error boundaries at the page level" with "user-friendly fallback UI"; no external error tracking — structured console.error suffices.)*
- [x] CHK005 — Is the RecordingPlayer component's control set fully specified — are play/pause/seek/speed/volume/fullscreen controls listed as requirements, or only implied by "playback" in FR-028? [Completeness, Spec §FR-028] *(Resolved in spec-addendum §9.1: Full player controls — play/pause, seek bar, speed selector (0.5x/1x/1.5x/2x), volume slider, fullscreen toggle. Keyboard shortcuts: Space=play/pause, Arrow keys=seek ±5s.)*
- [x] CHK006 — Are requirements defined for the TriageActions component's state after a first-write-wins rejection — does it show a retry option, permanently disable, or refresh the anomaly status? [Completeness, Spec §FR-041, Tasks §T075] *(Resolved in spec-addendum §5.4: On 409, show toast "Already {status} by {instructor} at {time}", refresh anomaly from server, re-enable actions with updated status.)*
- [x] CHK007 — Is the AddCameraDialog's local device selection (navigator.mediaDevices) specified as a firm requirement or a conditional feature ("if supported")? Are fallback requirements defined when the browser denies permissions? [Clarity, Spec §FR-006, Tasks §T099] *(Resolved in spec-addendum §16.4: Camera dialog validates RTSP URL. Local device selection is "if supported". Permission denied: show instructions link. Focus on RTSP as primary input.)*

## State Management Completeness

- [x] CHK008 — Are requirements defined for how the 6 Zustand stores (auth, camera, detection, anomaly, theme, health) handle stale data on tab re-focus — should stores refetch, use cached data, or show a stale indicator? [Completeness, Gap] *(Resolved in spec-addendum §8.5: Stale data on tab re-focus deferred to v2. V1: stores retain cached data. WS reconnects on tab focus via visibilitychange event, which refreshes real-time data.)*
- [x] CHK009 — Is the detection store's frame buffer size requirement specified with a concrete maximum — how many frames per camera are retained in memory before eviction, and what eviction strategy (FIFO, time-window) is used? [Clarity, Tasks §T051, Gap] *(Resolved in spec-addendum §6.3: 1 frame per camera (latest only). FIFO eviction. No accumulation — each new detection.frame replaces previous for that camera.)*
- [x] CHK010 — Are requirements defined for optimistic update rollback in the anomaly store — specifically what UI state is restored when a triage action returns a first-write-wins 409, and whether a toast/notification accompanies the rollback? [Completeness, Tasks §T070, Spec §FR-041] *(Resolved in spec-addendum §5.4: Optimistic update — on 409, restore previous status in store, show warning toast with conflict details, re-fetch anomaly from server.)*
- [x] CHK011 — Is the camera store's grid layout computation algorithm specified — does FR-007's breakpoint table (1→full, 2→1×2, etc.) define the complete rule set, or are intermediate states (e.g., 3 cameras in a 2×2 with one empty cell) left to implementer discretion? [Clarity, Spec §FR-007, Tasks §T050] *(Addressed: FR-007 defines explicit grid rules: 1=full, 2=1×2, 3-4=2×2, 5-6=2×3, 7-8=2×4. Below 1200px columns reduce by one tier. 3 cameras uses 2×2 per this rule.)*
- [x] CHK012 — Are requirements defined for cross-store dependencies — specifically how the anomaly store reacts when the camera store disconnects a camera mid-session (clear alerts? keep historical? show "camera offline" on alerts)? [Completeness, Gap] *(Resolved in spec-addendum §12.1: Keep historical alerts, add "camera offline" badge on affected alerts. Anomaly store does not clear — alerts remain for triage.)*

## API Contract Alignment

- [x] CHK013 — Does the frontend tasks-frontend.md auth API service (T035) account for all four REST contract endpoints (login, logout, getMe, updateProfile), and are the request/response shapes from rest-api.md traceable to TypeScript interfaces in T014? [Consistency, Tasks §T035, Contracts §rest-api.md] *(Addressed: rest-api.md defines all 4 endpoints with full JSON schemas. Frontend api/auth.ts already implements login, logout, getMe, changePassword. TypeScript types/api.ts has matching interfaces.)*
- [x] CHK014 — Is the dashboard API service (T036) consuming `GET /api/v1/dashboard/` defined with the expected response shape in the REST contract — specifically the `DashboardStats` type in T014? Does the contract define what "recent anomaly alerts (last 24 hours)" returns? [Completeness, Tasks §T036, Contracts §rest-api.md] *(Resolved: rest-api.md §Dashboard defines GET /api/v1/dashboard/ response with active_sessions, total_cameras, online_cameras, active_anomalies, recent_anomalies[], system_health. Response shape fully documented.)*
- [x] CHK015 — Are all WebSocket message types from websocket-api.md (detection.frame, prediction.update, anomaly.alert, anomaly.status_change, camera.status, health.update, storage.warning, error) represented in the frontend TypeScript interfaces (T015) and handled by corresponding hooks? [Consistency, Tasks §T015, Contracts §websocket-api.md] *(Addressed: websocket-api.md defines 13 message types with full JSON schemas. Frontend types/websocket.ts and hooks/useDetectionSocket.ts, useAnomalySocket.ts already exist. Contracts are canonical.)*
- [x] CHK016 — Is the `filter.update` client→server WebSocket message (informational, per contract) clearly documented as not affecting server-side detection push behavior — could a frontend developer mistakenly expect server-side filtering? [Clarity, Contracts §websocket-api.md, Client→Server §filter.update] *(Addressed: websocket-api.md explicitly states: "Filters are applied client-side on the Canvas rendering loop. This message is informational for server-side logging/analytics only. The server continues pushing all detections regardless of filter state.")*
- [x] CHK017 — Are pagination parameters (page, page_size, ordering) specified for all list API calls the frontend makes (cameras, sessions, anomalies, recordings, detection frames), and are default page sizes defined? [Completeness, Contracts §rest-api.md Common Patterns] *(Addressed: rest-api.md defines StandardResultsPagination (page_size=20, max=100, page_size_query_param). FR-073 defines admin activity page sizes 10-100/All with default 50. Plan §core/pagination.py provides the DRF pagination class.)*
- [x] CHK018 — Does the export API flow (POST initiate → GET poll status → GET download) have clearly defined polling interval requirements, maximum poll duration, and timeout/failure display requirements? [Completeness, Tasks §T087, Contracts §rest-api.md §Session Export] *(Resolved in spec-addendum §9.3: Poll every 2s, max 10min, timeout shows error toast. Failed status shows retry button. Download URL valid for 1 hour.)*

## WebSocket & Real-Time Requirements

- [x] CHK019 — Is the WebSocket reconnection strategy (1s→2s→4s→8s→16s max) specified consistently between websocket-api.md and tasks-frontend.md T017 — and are both aligned with the spec's FR-010 reconnection behavior (5 retries at 5-second intervals for cameras)? [Consistency, Spec §FR-010, Contracts §websocket-api.md, Tasks §T017] *(Addressed: These are two separate systems. FR-010 = RTSP camera reconnection (5 retries, exponential backoff). FR-019/websocket-api.md = WebSocket reconnection (10 retries, 1s→16s backoff). Asymmetry is intentional — different protocols, different failure modes.)*
- [x] CHK020 — Are requirements defined for what the frontend displays during WebSocket reconnection attempts — is there a per-attempt status update (attempt 3/10), a progress indicator, or only the final "Connection Lost" banner after 10 failures? [Completeness, Tasks §T017, Gap] *(Resolved in spec-addendum §7.2: Show reconnecting banner with attempt counter "Reconnecting... (attempt 3/10)". After all retries: "Connection Lost — Please refresh the page.")*
- [x] CHK021 — Are requirements defined for WebSocket message ordering and deduplication — if detection.frame messages arrive out-of-order during reconnection, should the frontend discard older frames, reorder by timestamp, or display as-received? [Completeness, Gap] *(Resolved in spec-addendum §7.3: Display as-received (latest wins). Deduplication via frame_number — if frame_number ≤ last seen for that camera, skip.)*
- [x] CHK022 — Is the WebSocket auto-resubscribe requirement (T017) specified with behavior for partial subscription failure — if 3 of 5 camera subscriptions succeed after reconnect, what does the UI show for the 2 failures? [Completeness, Tasks §T017, Gap] *(Resolved in spec-addendum §7.4: Per-camera subscription status. Failed cameras show "Subscription Failed" badge. Successful cameras resume normally. Retry button per failed camera.)*
- [x] CHK023 — Are requirements defined for the detection WebSocket's throughput capacity — with ~15 msg/s per camera × 8 cameras = ~120 msg/s, is message batching or throttling specified on the frontend receive side? [Completeness, Plan §Performance Goals, Gap] *(Addressed: Spec Assumptions §Detection processing rate + Clarification Round 3 define backend micro-batching (100ms windows) reducing WS messages from ~120/sec to ~8-10/sec. Frontend receives manageable volume.)*
- [x] CHK024 — Is the ping/pong health monitor (send ping every 30s, reconnect if no pong in 10s) specified with behavior when the monitor detects a timeout — does it trigger reconnection immediately or wait for the next scheduled ping? [Clarity, Contracts §websocket-api.md §Reconnection Strategy] *(Addressed: websocket-api.md §Reconnection Strategy step 5: "Health monitor: ping every 30 seconds, reconnect if no pong in 10 seconds." Implicit immediate reconnection on timeout — "reconnect" is the action, not "schedule next ping.")*

## Canvas & Video Rendering Requirements

- [x] CHK025 — Is the Canvas overlay coordinate system specified — are bounding box coordinates (bbox: [x, y, w, h] per contract) in pixel space matching the video element dimensions, or do they require scaling/transform from source resolution to display resolution? [Clarity, Contracts §websocket-api.md §detection.frame, Tasks §T054] *(Resolved in spec-addendum §6.2: Coordinates in source resolution pixel space. Frontend scales via ratio: displayWidth/sourceWidth, displayHeight/sourceHeight. Source resolution from camera metadata.)*
- [x] CHK026 — Are requirements defined for Canvas rendering when the video element is resized (browser resize, grid layout change, fullscreen toggle) — should the canvas immediately re-scale bbox coordinates, and is there a flicker tolerance? [Completeness, Tasks §T054, Gap] *(Resolved in spec-addendum §6.2: ResizeObserver on video element. Canvas dimensions match video on every resize. Re-scale bboxes immediately. No flicker tolerance specified — synchronous update on resize callback.)*
- [x] CHK027 — Is the Canvas drawing color specification complete — are hex values defined for ALL detection classes (Student=#22D3EE, Teacher=#A78BFA per R-009), or only for student/teacher? What colors are used for Standing, Sitting, Looking Left, etc.? [Completeness, Research §R-009, Spec §FR-011] *(Resolved in spec-addendum §1.1: All 8 detection class colors defined — Student=#22D3EE, Teacher=#A78BFA, Standing=#F59E0B, Sitting=#10B981, Looking_Left=#EC4899, Looking_Right=#8B5CF6, Looking_Down=#6366F1, Hand_Raised=#EF4444.)*
- [x] CHK028 — Are requirements defined for the maximum number of simultaneous bounding boxes the Canvas should render without frame drops — with up to 8 cameras × N students per camera, is there a cap or degradation strategy? [Completeness, Gap, Plan §Performance Goals] *(Resolved in spec-addendum §6.3: Max 200 bounding boxes total across all cameras. Beyond 200: skip lowest-confidence boxes. Per-camera cap = 200/active_cameras.)*
- [x] CHK029 — Is the "click-to-highlight" behavior (FR-018, T078) specified with visual treatment details — thicker border width (how many px?), pulsing glow animation (duration, easing?), and auto-dismiss timing (how long does highlight persist)? [Clarity, Spec §FR-018, Tasks §T078] *(Resolved in spec-addendum §5.7: 3px border + box-shadow glow. Animation: pulse 1s ease-in-out infinite. Auto-dismiss after 5s. Click again to dismiss early.)*
- [x] CHK030 — Are requirements defined for the WHEP WebRTC client's error states — what does the UI show when go2rtc is unreachable, when SDP negotiation fails, or when the MediaStream track ends unexpectedly? [Completeness, Tasks §T052, Gap] *(Resolved in spec-addendum §4.3: go2rtc unreachable="Stream Unavailable" overlay. SDP failure="Connection Failed — Retrying..." with exponential backoff. Track ended="Camera Disconnected" with reconnect.)*
- [x] CHK031 — Is the recording playback Canvas overlay data-fetching strategy specified — does RecordingPlayer fetch detection frames for the entire recording upfront, stream them as playback progresses, or fetch windowed chunks around the current playback position? [Clarity, Tasks §T085, Spec §FR-028] *(Resolved in spec-addendum §3.6: Windowed chunk strategy — fetch 30s window around current playback position. Pre-fetch next chunk when 80% through current. Cache last 2 chunks.)*

## Theme System Requirements

- [x] CHK032 — Are the complete CSS custom property token names listed for all three themes, or only described generically as "color, spacing, typography, radius, shadow, transitions" (T008)? Are token names consistent across all references (spec, research R-009, tasks)? [Completeness, Tasks §T008, Research §R-009] *(Resolved in spec-addendum §1.1: Complete token tables with CSS custom property names for all 3 themes — color, spacing, typography, radii, shadows, transitions. Token names standardized.)*
- [x] CHK033 — Is the theme persistence priority chain (localStorage → user profile theme_preference → "black-purple" default) clearly specified as a requirement, or only in tasks (T095)? Is the spec-level requirement (FR-023) limited to localStorage only? [Consistency, Spec §FR-023, Tasks §T095] *(Addressed: FR-023 specifies localStorage persistence. Data-model User.theme_preference stores server-side. PATCH /auth/me/ allows updating. Full chain: localStorage for immediate + profile API for cross-device persistence + default fallback.)*
- [x] CHK034 — Are requirements defined for theme-to-backend sync failure — if PATCH /auth/me/ fails when saving theme preference, does the frontend silently continue with localStorage, show an error, or retry? [Completeness, Tasks §T092, Gap] *(Resolved in spec-addendum §8.5: Silently continue with localStorage on sync failure. No error toast — theme is a non-critical preference. Retry on next theme change.)*
- [x] CHK035 — Is the FOUC-prevention mechanism (T013) specified as a firm requirement in the spec, or only in tasks? Are requirements defined for what the user sees if JavaScript is disabled or the inline script fails? [Completeness, Tasks §T013, Gap] *(Resolved in spec-addendum §1.2: FOUC prevention is a firm requirement. Inline <script> in index.html reads localStorage theme before first paint. JS disabled: falls back to default theme CSS.)*
- [x] CHK036 — Are specific WCAG 2.1 AA contrast ratio targets documented per element type (body text ≥4.5:1, large text ≥3:1, UI components ≥3:1) for each of the three themes, or is FR-024 a blanket statement without per-theme verification criteria? [Measurability, Spec §FR-024, SC-010] *(Resolved in spec-addendum §11.1: Per-element type targets documented. Verification via axe-core automated testing per theme. CI runs contrast checks on all 3 themes.)*

## Routing, Navigation & Auth

- [x] CHK037 — Are requirements defined for deep-link behavior — if an unauthenticated user navigates directly to /recordings/abc-123, should the route guard redirect to /login and then return to /recordings/abc-123 after successful auth? [Completeness, Tasks §T039, Gap] *(Resolved in spec-addendum §8.3: Yes — route guard stores intended URL in sessionStorage, redirects to /login, restores after auth. Deep links preserved.)*
- [x] CHK038 — Is the session timeout behavior (FR-004: "configurable inactivity timeout, default 30 minutes") specified for the frontend — does the frontend detect timeout via a 401 response, a timer, or a WebSocket disconnect? What UI is shown? [Clarity, Spec §FR-004, Gap] *(Resolved in spec-addendum §8.2: Dual detection — 401 response triggers immediate redirect to /login. Proactive: frontend timer at 29min shows "Session expiring in 1 minute" warning banner. Interaction resets timer.)*
- [x] CHK039 — Are requirements defined for route transitions — do page changes use loading indicators, skeleton screens, or instant rendering? Is there a consistent route-level loading pattern? [Completeness, Spec §FR-034, Gap] *(Resolved in spec-addendum §4.2: Route transitions use skeleton screens for data-dependent pages (Dashboard, Sessions, Recordings). Instant render for static pages (Settings, Login).)*
- [x] CHK040 — Is the keyboard navigation scope (FR-035) specified with concrete shortcut assignments — which keys map to which actions (e.g., Ctrl+1–8 for camera focus per T109)? Are these documented in the spec or only in tasks? [Completeness, Spec §FR-035, Tasks §T109] *(Resolved in spec-addendum §11.3: Keyboard shortcuts table — 12 actions: Ctrl+1-8=camera focus, Space=play/pause, Esc=close modal, Tab=navigate, ?=show shortcuts help.)*

## Error Handling & Edge Cases (Frontend-Specific)

- [x] CHK041 — Are frontend requirements defined for the "Detection Service Unavailable" edge case — how does the Canvas overlay behave when detection.frame messages stop arriving? Does it show the last known boxes, clear the overlay, or display a "no detections" message? [Completeness, Spec §Edge Cases, Gap] *(Addressed: Spec Edge Cases: "camera feed MUST continue displaying raw video without bounding boxes, and a status banner MUST indicate 'Detection Service Unavailable — Raw Feed Only.'" Canvas clears overlays; video continues.)*
- [x] CHK042 — Are requirements defined for handling corrupted or malformed WebSocket messages — if a detection.frame arrives with missing fields or invalid JSON, does the frontend silently skip it, log an error, or show a warning? [Completeness, Gap] *(Resolved in spec-addendum §7.3: Silently skip malformed messages. Log via console.warn in development. No user-visible warning — next valid frame overwrites.)*
- [x] CHK043 — Is the multi-tab behavior (spec edge case) specified with frontend-specific requirements — if the same instructor has two tabs, do both receive WebSocket anomaly.status_change broadcasts, and is there a risk of duplicate triage attempts? [Completeness, Spec §Edge Cases §multi-tab, Gap] *(Addressed: Spec Edge Cases §multi-tab: "System MUST support multiple tabs with independent camera configurations." First-write-wins (FR-041/FR-042) prevents duplicate triage. anomaly.status_change broadcasts to all connections.)*
- [x] CHK044 — Are requirements defined for what the frontend shows during the gap between "session started" and "first detection.frame received" — is this a loading state, an empty canvas, or a "Waiting for detections..." message? [Completeness, Gap] *(Resolved in spec-addendum §4.1: "Waiting for detections..." message with subtle pulse animation on empty canvas overlay. Video feed visible underneath.)*
- [x] CHK045 — Are requirements defined for the student re-identification edge case from the frontend perspective — when a tracking ID changes, how is the PredictionsPanel updated (new card? merged card? notification?), and does BoundingBoxCanvas show a visual transition? [Completeness, Spec §Edge Cases §re-identification, Tasks §T073] *(Resolved in spec-addendum §12.9: Backend sends tracking_update WS message with old_id→new_id mapping. Frontend merges cards — old card fades out, new card inherits history. Canvas: smooth bbox transition over 300ms.)*
- [x] CHK046 — Is the bandwidth degradation edge case specified with frontend-specific behavior — does the frontend detect frame drops, measure incoming FPS, and initiate resolution reduction, or is this entirely backend-controlled? [Clarity, Spec §Edge Cases §bandwidth, Gap] *(Resolved in spec-addendum §12.10: Frontend detects via frame timestamp gaps. If measured FPS <5 for 10s, show "Low Bandwidth" badge. Resolution reduction is backend-controlled — frontend observes only.)*
- [x] CHK047 — Are requirements defined for the export flow's error states — what does ExportButton show if the POST initiate fails (500), if polling detects a failed export, or if the download link expires? [Completeness, Tasks §T087, Gap] *(Resolved in spec-addendum §9.3: POST 500="Export failed, please try again" toast. Polling detects FAILED="Export failed: {reason}" with retry button. Download expired=re-initiate export.)*

## Testing Requirements Quality

- [x] CHK048 — Is the ≥80% coverage gate (Constitution Principle II) specified with per-category breakdown — is 80% measured across all frontend code combined, or must each category (stores, hooks, components, pages) individually meet 80%? [Clarity, Plan §Constitution Check §II] *(Resolved in spec-addendum §13.4: Single aggregate 80% across all frontend code. Not per-category. CI gate blocks merge below 80%.)*
- [x] CHK049 — Are E2E test scenarios (T114–T117) traceable to spec acceptance scenarios — specifically, do the 4 Playwright tests collectively cover all 5 US1 acceptance scenarios, all 4 US2 scenarios, and the key US4/US6 scenarios? [Coverage, Tasks §T114–T117, Spec §Acceptance Scenarios] *(Resolved in spec-addendum §15.2: E2E traceability matrix mapping T114→US1 S1-S5, T115→US2 S1-S4, T116→US4 S1-S3, T117→US6 S1-S2.)*
- [x] CHK050 — Are requirements defined for mock strategy in unit tests — should WebSocket hooks be tested with mock servers (e.g., mock-socket), Zustand stores with mock API responses, and Canvas components with jsdom canvas stubs? [Completeness, Gap] *(Resolved in spec-addendum §15.1: Mock strategy table — WS: vi.fn mock handlers, Stores: direct setState, API: MSW (Mock Service Worker), Canvas: jest-canvas-mock.)*
- [x] CHK051 — Is the TDD mandate ("tests MUST fail before implementation") verifiable — are there CI pipeline requirements that enforce test-first order, or is this a process guideline only? [Measurability, Tasks §Phase 3 Notes, Gap] *(Resolved in spec-addendum §15.3: Process guideline only. No CI enforcement of test-first order. Verified via code review convention.)*

## Performance Requirements (Frontend-Specific)

- [x] CHK052 — Is the "≥15 FPS" Canvas rendering target (Plan §Performance Goals, SC-004) specified with a measurement method — is it measured via requestAnimationFrame callback timing, Chrome DevTools FPS overlay, or a custom performance observer? [Measurability, Plan §Performance Goals, SC-004] *(Resolved in spec-addendum §13.1: Measured via PerformanceObserver + rAF callback timing. Dev mode FPS counter component. CI: warn <15, fail <10.)*
- [x] CHK053 — Is the "<200ms UI interactions" target (Plan §Performance Goals) defined for which specific interactions — all button clicks, filter toggles, store dispatches, or a curated list? Is the measurement from user input event to visual feedback? [Clarity, Plan §Performance Goals, Spec §SC-003] *(Resolved in spec-addendum §13.2: Scoped list — button clicks, filter toggles, panel open/close, triage actions, camera grid switch. Measured from pointerup/keyup to next paint via PerformanceObserver.)*
- [x] CHK054 — Are requirements defined for initial page load performance — is there a time-to-interactive budget, a JavaScript bundle size cap, or a Lighthouse score target for the frontend SPA? [Completeness, Gap] *(Resolved in spec-addendum §13.3: Page load budget — 500KB JS (gzipped), 50KB CSS, 30KB fonts. TTI target <3s on 4G. No Lighthouse score target in v1.)*
- [x] CHK055 — Is the WebSocket throughput handling requirement (~120 msg/s across 8 cameras) specified with a frontend processing strategy — should messages be batched into requestAnimationFrame cycles, debounced per camera, or processed inline? [Clarity, Plan §Performance Goals, Gap] *(Resolved in spec-addendum §7.5: rAF batching — accumulate messages in buffer, process latest per camera per animation frame. Discard stale frames.)*
- [x] CHK056 — Is the "<500ms recording seek" target (SC-008) specified with frontend requirements — does this include video element seek + canvas overlay data-fetch + re-render, or just the video seek time? [Clarity, Spec §SC-008] *(Resolved in spec-addendum §9.2: Includes full pipeline — video seek + detection chunk fetch + canvas re-render. Target <500ms total including chunk fetch. Pre-fetched chunks excluded from budget.)*

## Accessibility Requirements (Frontend-Specific)

- [x] CHK057 — Are ARIA live region requirements defined for real-time content — specifically, should anomaly alerts use `aria-live="assertive"`, detection counts use `aria-live="polite"`, and camera status changes use `role="status"`? [Completeness, Gap] *(Resolved in spec-addendum §11.2: Exact ARIA assignments — anomaly alerts=assertive, detection counts=polite, camera status=assertive, toast=polite, export progress=polite.)*
- [x] CHK058 — Are skip-navigation requirements defined — is a "Skip to main content" link required at the top of every page, and does it bypass the Header and Sidebar? [Completeness, Spec §FR-035, Tasks §T109] *(Resolved in spec-addendum §11.4: Yes — "Skip to main content" link as first focusable element. Bypasses Header + Sidebar. Visible on focus only. Links to #main-content.)*
- [x] CHK059 — Are requirements defined for the `prefers-reduced-motion` media query — should theme transitions, bounding box pulse animations, and loading spinners respect this preference? [Completeness, Gap] *(Resolved in spec-addendum §11.5: Yes — all animations gated by prefers-reduced-motion. Reduced motion: instant theme switch, no pulse glow, static spinner replaced with text "Loading...".)*
- [x] CHK060 — Are focus management requirements defined for modals (AddCameraDialog, dismiss reason) — is focus trapped inside the modal, returned to the trigger element on close, and announced to screen readers? [Completeness, Tasks §T020, Gap] *(Resolved in spec-addendum §11.6: Focus trap inside modal. Return to trigger on close. Screen reader: role=dialog, aria-labelledby, aria-describedby. Escape closes.)*

## Dependencies & Assumptions

- [x] CHK061 — Is the assumption "Backend API endpoints exist and are functional" (tasks-frontend.md header) validated — are mock server requirements or API stub configurations defined for frontend-only development? [Assumption, Tasks §Header] *(Resolved in spec-addendum §15.4: MSW (Mock Service Worker) for frontend-only development. Mock handlers defined for all API endpoints. Enabled via VITE_USE_MOCKS=true env var.)*
- [x] CHK062 — Is the go2rtc WHEP endpoint URL format documented in the contracts — the WebSocket contract shows `wss://{host}/ws/...` patterns, but the WHEP endpoint for WebRTC SDP exchange (`POST /api/webrtc`) is only mentioned in tasks (T052), not in rest-api.md? [Gap, Tasks §T052, Contracts §rest-api.md] *(Addressed: Clarification Round 2 Feb 28 defines canonical URL: `/whep/{camera_id}/` — nginx proxies to `http://go2rtc:1984/api/webrtc?src={camera_id}`. FR-008 documents proxy + auth subrequest. Not in rest-api.md because it's a proxy pass, not a Django endpoint.)*
- [x] CHK063 — Is the dependency between frontend detection store and backend detection processing rate (~10–15 fps) documented with frontend buffer requirements — how much memory should the frontend allocate for detection frame buffering across 8 cameras? [Dependency, Spec §Assumptions, Tasks §T051] *(Resolved in spec-addendum §6.3: 1 frame per camera (latest only). 8 cameras × 1 frame × ~2KB = ~16KB max buffer. No accumulation. Memory ceiling negligible.)*

## Ambiguities & Conflicts

- [x] CHK064 — Does the spec's camera reconnection (FR-010: "5 retries at 5-second intervals") conflict with the WebSocket reconnection strategy (contracts: "exponential backoff 1s→16s, 10 retries")? Are these two independent systems, and is the frontend responsible for both? [Conflict, Spec §FR-010, Contracts §websocket-api.md] *(Resolved: Two independent systems. FR-010 = RTSP camera reconnection (exponential backoff 1s→16s, 5 retries). FR-019/websocket-api.md = WebSocket data channel (1s→16s, 10 retries). Frontend manages both independently through useWhepClient and useWebSocket hooks.)*
- [x] CHK065 — Is the "Predictions Page" (PredictionsPage.tsx per plan) a standalone page or an overlay/panel on the Camera Feed page? The spec describes predictions "alongside the camera feed" (US-4), but the plan lists it as a separate page route. [Ambiguity, Spec §US-4, Plan §pages/PredictionsPage.tsx] *(Resolved: Clarification Round 3: "Yes, both modes are required per FR-016. The sidebar provides at-a-glance monitoring alongside live feeds; the page allows detailed analysis." Both collapsible sidebar on CameraFeedPage AND dedicated PredictionsPage.)*
- [x] CHK066 — Is the Settings page content (T108) fully specified in requirements — the spec mentions theme switching and profile display, but are "session timeout info" and "keyboard shortcuts reference" derived from spec FRs or added in tasks without spec backing? [Traceability, Tasks §T108, Gap] *(Addressed: Spec FR-023 = theme switching, FR-070 = password change from profile/settings, PATCH /auth/me/ = profile update, FR-004 = session timeout config. Core settings page content is spec-backed. Additional items in tasks are implementer enhancements.)*
- [x] CHK067 — Does the tasks' "auth API" base path (`POST /auth/login/`) align with the REST contract's base URL (`/api/v1/auth/login/`) — is the `api/v1` prefix included or stripped by the HTTP client (T016)? [Consistency, Tasks §T035, Contracts §rest-api.md] *(Addressed: rest-api.md declares "Base URL: /api/v1/". Frontend api/client.ts configures axios baseURL to include the /api/v1/ prefix. All endpoint paths in tasks are relative to this base. Consistent.)*

---

## Notes

- Check items off as completed: `[x]`
- Add inline findings or comments next to each item
- Reference specific spec sections, contract definitions, or task IDs when documenting gaps
- Items marked `[Gap]` indicate requirements that may need to be added to spec.md or plan.md
- Items marked `[Conflict]` require resolution before implementation begins
- Items marked `[Ambiguity]` need clarification from the spec author
- This checklist complements [requirements.md](requirements.md) (general spec quality) and [ux.md](ux.md) (UX/UI requirements quality)
