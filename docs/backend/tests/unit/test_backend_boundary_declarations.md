# test_backend_boundary_declarations.py

## Purpose

Validates that each backend app publishes a typed `ModuleBoundaryRecord` with public inputs, outputs, consumers, forbidden dependencies, and failure behavior.
