# RuntimePage

Runtime dashboard now includes:

- Replay controls for selected student tracks (`Replay points`).
- Extended frame timeline observability: queue depth, overflow count, GPU utilization, memory.
- Extended summary KPI cards: average GPU utilization, memory, and queue depth.
